{
  "ignoreCommand": "git fetch --depth=2 origin $VERCEL_GIT_COMMIT_SHA && git diff --quiet $VERCEL_GIT_PREVIOUS_SHA $VERCEL_GIT_COMMIT_SHA -- apps/web/ packages/api/ packages/auth/ packages/env/ packages/config/ packages/ui/"
}